from setuptools import find_packages, setup

Install_require = [
    "rich",
    "rich-cli",
    "xonsh",
    "wheel",
    "asciimatics",
    "requests"
]

setup (
    author="Bayu Riski A.M",
    install_requires=Install_require
)
